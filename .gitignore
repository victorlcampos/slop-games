node_modules/

# gerado por `npm run build` (e pelo CI, que publica no Pages)
dist/

.DS_Store

# rascunhos de automação (script agendado, mensagem de commit em preparo)
.agendado.sh
.commit-msg.tmp
*.tmp.mjs
