name: Test and publish

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# One publish at a time; it does not cancel the one already going out.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  # ---------------------------------------------------------------------------
  # Builds and tests. The artifact that goes up from here is exactly the one
  # that was tested — the publishing job rebuilds nothing, it only publishes.
  # That way there is no chance of shipping a build different from the one that
  # passed.
  # ---------------------------------------------------------------------------
  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      # The index reads the first commit that added each game. A shallow clone
      # turns every card into "added today", so the build needs the whole story.
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Build the games and the index
        run: npm run build

      # The build regenerates omarchy/Catalog.js, which — unlike everything else
      # it writes — is committed, because the Omarchy plugin is a clone of this
      # repository and a clone never builds. That also means the test that says
      # "the catalog is in step" cannot fail here: the build has already put it
      # back in step. This is the check that does fail, and it is the only place
      # in the gate that looks at the working tree instead of at dist/.
      - name: The plugin's catalog is committed and current
        run: git diff --exit-code omarchy/Catalog.js

      - name: Unit and catalog tests
        run: npm test

      # Every game's own test, and none of them opens a browser: the runner has
      # no graphics card, and a scenario that played a 3D game there measured
      # SwiftShader instead of the code. What plays the games now is Node —
      # the physics, the catalogue, the battle and the street index are all
      # plain functions (CLAUDE.md, section 6).
      - name: Each game's own tests
        run: npm run test --workspaces --if-present

      - name: Package the tested dist
        if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  # ---------------------------------------------------------------------------
  # Only publishes if the tests passed, and only from main.
  # ---------------------------------------------------------------------------
  publish:
    name: Publish to Pages
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        uses: actions/deploy-pages@v5
